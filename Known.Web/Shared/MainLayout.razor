@inherits LayoutComponentBase

<div class="header">
    <h1>Known管理系统</h1>
</div>

<div class="main">
    <div class="sidebar">
        <NavMenu Items="@GetMenuItems()" />
    </div>

    <div class="content">
        @Body
    </div>
</div>

@code {
    private IEnumerable<MenuItem> GetMenuItems()
    {
        var ret = new List<MenuItem>
        {
            new MenuItem() { Text = "开发平台", Icon = "oi oi-monitor" },
            new MenuItem() { Text = "系统管理", Icon = "oi oi-cog" },
            new MenuItem() { Text = "标准页面", Icon = "oi oi-browser" }
        };

        ret[0].AddItem(new MenuItem() { Text = "资源管理", Icon = "oi oi-list", Url = "/" });
        ret[0].AddItem(new MenuItem() { Text = "代码生成", Icon = "oi oi-code", Url = "/counter" });

        ret[1].AddItem(new MenuItem() { Text = "数据字典", Icon = "oi oi-spreadsheet", Url = "/fetchdata" });
        ret[1].AddItem(new MenuItem() { Text = "角色管理", Icon = "oi oi-people" });
        ret[1].AddItem(new MenuItem() { Text = "用户管理", Icon = "oi oi-person" });

        ret[2].AddItem(new MenuItem() { Text = "NotFound", Icon = "oi oi-ban", Url = "/notfound" });
        ret[2].AddItem(new MenuItem() { Text = "Error", Icon = "oi oi-x", Url = "/error" });

        return ret;
    }
}